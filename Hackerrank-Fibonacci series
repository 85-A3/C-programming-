#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int N;
    scanf("%d", &N);

    if (N <= 0) {
    } else if (N == 1) {
        printf("0\n");
    } else {
        int a = 0;
        int b = 1;
        int c;
        printf("%d %d", a, b);
        for (int i = 3; i <= N; i++) {
            c = a + b;
            printf(" %d", c);
            a = b;
            b = c;
        }
        printf("\n");
    }

    return 0;
}
